# Data directory

- `dhaka_graph.graphml` — cached OSM road graph (generated on first run; gitignored).
- `dhaka_edges.parquet` — vectorized synthetic edge attributes (gitignored).
- `landmarks.yaml` — named places → coordinates (tracked).
